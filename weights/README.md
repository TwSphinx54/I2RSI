# Here are weights for system using.
## File Structure are as follows:

    --change_detection  
        --aircraft  
        --oiltank  
        --overpass  
        --playground  
    --object_classification  
    --object_detection  
    --object_extraction

Each bottom folder contains 5 files to describe the model used in the prediction:  

    model.pdiparams
    model.pdiparams.info
    model.pdmodel
    model.yml
    pipeline.yml
